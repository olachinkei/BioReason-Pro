# Design Docs

These docs explain stable design intent. They should stay shorter than the code.

## Documents

- `core-beliefs.md`: principles for this Senpai target.
- `senpai-screening-contract.md`: how the 5-step gate is supposed to behave.
- `data-generation.md`: how generated data artifacts are rebuilt.

## Related

- ADRs live in `docs/adr/`.
- Execution history lives in `docs/exec-plans/`.
- External method references live in `docs/references/`.
